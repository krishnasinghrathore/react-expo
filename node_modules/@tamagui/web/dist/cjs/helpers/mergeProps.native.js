"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: !0 });
}, __copyProps = (to, from, except, desc) => {
  if (from && typeof from == "object" || typeof from == "function")
    for (let key of __getOwnPropNames(from))
      !__hasOwnProp.call(to, key) && key !== except && __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: !0 }), mod);
var mergeProps_exports = {};
__export(mergeProps_exports, {
  mergeProps: () => mergeProps
});
module.exports = __toCommonJS(mergeProps_exports);
var import_useMedia = require("../hooks/useMedia"), import_pseudoDescriptors = require("./pseudoDescriptors"), mergeProps = function(a, b, inverseShorthands) {
  var out = {};
  for (var key in a)
    mergeProp(out, a, b, key, inverseShorthands);
  if (b)
    for (var key1 in b)
      mergeProp(out, b, void 0, key1, inverseShorthands);
  if (b && Object.keys(b).length > 0) {
    var hasPropsNeedingReorder = Object.keys(b).some(function(key4) {
      return (key4 in import_pseudoDescriptors.pseudoDescriptors || key4 === "variant") && key4 in a && key4 in out;
    });
    if (hasPropsNeedingReorder) {
      var reordered = {};
      for (var key2 in b)
        (key2 in import_pseudoDescriptors.pseudoDescriptors || key2 === "variant") && key2 in out && (reordered[key2] = out[key2]);
      for (var key3 in out)
        key3 in reordered || (reordered[key3] = out[key3]);
      return reordered;
    }
  }
  return out;
};
function mergeProp(out, a, b, key, inverseShorthands) {
  var longhand = (inverseShorthands == null ? void 0 : inverseShorthands[key]) || null, val = a[key];
  if (key in import_pseudoDescriptors.pseudoDescriptors || import_useMedia.mediaKeys.has(key)) {
    out[key] = {
      ...out[key],
      ...val
    };
    return;
  }
  b && (key in b || longhand && longhand in b) || (out[longhand || key] = val);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  mergeProps
});
//# sourceMappingURL=mergeProps.js.map
